-- Create extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Optional: Create additional schemas or perform initial setup
-- Example: CREATE SCHEMA IF NOT EXISTS frontendmu;

-- Print initialization message
SELECT 'Database initialized for Frontend.mu' AS init_message;